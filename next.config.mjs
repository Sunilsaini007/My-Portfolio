/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.dummyjson.com',
      'i.dummyjson.com',
      'robohash.org',
      'dummyjson.com',
    ],
  },
};

export default nextConfig;
